The notebook called "SimSiam_ConvNextXLarge" is the one used for the final model developement. It was thought to be used in Kaggle.

The notebook called "Inference-Notebook" is the one used for making inferences. For using it (it was thought to be used in Kaggle) just import as a folder your model and import the dataset too in the inputs, and modify the lines related to the importing, so as to import your model and dataset.

The file called "labeling.py" was meant to look for the troll images (shrek and trolololo) in the dataset and to label them.

The file called "plot.py" was meant to inspect manually the dataset.
